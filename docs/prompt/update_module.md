我要开始 <学校详情> 模块的需求迭代。请只读扫描仓库，不修改业务代码。

# 迭代需求list
1. 显示学校基本信息，包括学校名称、所属区、学校类型、备注、住宿情况等信息（可以参考高中学校表字段）
2. 显示学校最近一年的招生情况：包括自招、到区、到校的总人数
3. 显示学校介绍（介绍内容，可以先直接在页面上默认填充一下，现在还没有数据）
4. 解决bug，现在这个页面在ios上显示，页面头部会被遮挡

# 任务：
1) 生成 docs/prd/changes/CR-YYYY-MM-DD-<模块名>.md 模板
2) 根据仓库现状，整理现有的逻辑，写需求变更文档
3) 根据需求变更文档，给出技术设计方案
4) 如何涉及到新API的，请给出新API的接口方案
5) 涉及到UI变化，给出组件修改的方案


# 输出要求：
- 给出创建的文件路径
- 给出需要修改的代码文件，以及改动逻辑，包括新增和修改
- 不要开始实现功能和改代码