我要开始 <学校详情> 模块的需求迭代。请只读扫描仓库，不修改业务代码。

# 迭代需求list
1. API修改，从自招表获取，自招（总人数，艺术生，体育生），到区（总人数），到校(总人数)，住宿等信息。
2. 前端：“学校介绍”卡片，放到页面的第一位。
3. 前端：“近一年招聘情况”：增加自招体育生人数和艺术生人数，样式做重新布局
4. 前端：“录取分数线”，剔出重复数据，样式和“近一年招聘情况”保持一致
5. 前端增加基于学生画像卡片（不需要操作按钮，其他保持一致），内容直接跟目标页一致，内容可以前端带过来
6. 学校基础数据卡片删除，包括：“学校全程、学校代码、住宿”等
7. “上海中考志愿与录取方式” 卡片删除
8. “录取概率分析”的卡片删除


# 任务：
1) 生成 docs/prd/changes/CR-YYYY-MM-DD-<模块名>.md 模板
2) 根据仓库现状，整理现有的逻辑，写需求变更文档
3) 根据需求变更文档，给出技术设计方案
4) 如何涉及到新API的，请给出新API的接口方案
5) 涉及到UI变化，给出组件修改的方案

# 参考：
涉及到数据库字段的，可以查看：db_init.sql

# 输出要求：
- 不要创建模块以外的文件，需求和设计都在同一个文档修改
- 给出现状和修改方案的对比
- 给出需要修改的代码文件（包括前端和后端），以及改动逻辑，包括新增和修改
- 不要开始实现功能和改代码