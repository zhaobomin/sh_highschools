我要开始 <学校详情> 模块的需求迭代。请只读扫描仓库，不修改业务代码。

# 迭代需求list
1. 恢复原来的NavBar，以及对应的功能，在上一个版本被你删除了
2. 学生画像下面，基于画像计算的“学校卡片”，内容和样式跟目标页“学校”列表一致（不需要操作按钮，其他保持一致）
3. “最近一年招生情况” 样式优化：3列2行，数字文本字体调小，并且不需要加粗突出
4. “录取分数线”样式优化：3列1行，顺序改成“统招”、“到区”、到校，数字文本字体调小，并且不需要加粗突出。


# 任务：
1) 生成 docs/prd/changes/CR-YYYY-MM-DD-<模块名-v版本号>.md 模板，文件名有重复，请对文件进行编码。
2) 根据仓库现状，整理现有的逻辑，写需求变更文档
3) 根据需求变更文档，给出技术设计方案
4) 如何涉及到新API的，请给出新API的接口方案
5) 涉及到UI变化，给出组件修改的方案

# 参考：
涉及到数据库字段的，可以查看：db_init.sql

# 输出要求：
- 不要创建模块以外的文件，需求和设计都在同一个文档修改
- 给出现状和修改方案的对比
- 给出需要修改的代码文件（包括前端和后端），以及改动逻辑，包括新增和修改
- 不要开始实现功能和改代码