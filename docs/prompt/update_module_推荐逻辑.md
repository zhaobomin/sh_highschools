我要开始 <筛选> 模块的需求迭代。请只读扫描仓库，不修改业务代码。

# 迭代需求list
1. 概率计算逻辑更新：以高中学校最近一年的平行志愿录取分数线为基准，- 下限分 > 平行志愿录取分数线 => "拿捏"
- 下限分 < 平行志愿录取分数线 and 上限分 > 平行志愿录取分数线 => "可冲"
- 上限分 < 平行志愿录取分数线 => "难"
2. 前端标签颜色：难（红色），拿捏（绿色）、可冲（黄色）


# 任务：
1) 生成 docs/prd/changes/CR-YYYY-MM-DD-<模块名>.md 模板
2) 根据仓库现状，整理现有的逻辑，写需求变更文档
3) 根据需求变更文档，给出技术设计方案
4) 如何涉及到新API的，请给出新API的接口方案
5) 涉及到UI变化，给出组件修改的方案


# 输出要求：
- 不要创建模块以外的文件，需求和设计都在同一个文档修改
- 给出创建的文件路径
- 给出需要修改的代码文件，以及改动逻辑，包括新增和修改
- 不要开始实现功能和改代码