我要开始 <评估> 模块的需求迭代。请只读扫描仓库，不修改业务代码。

# 迭代需求list
1. 渠道名额和分数对比功能，字段顺序：名额、分数线、差距(最好)、差距（最差）、差距（平均）、状态。
- 如果只有1次模拟考，就直接把三个差距替换为“模考差距”
- 状态，用红黄绿灯形式，差距5分以内绿灯、差距20分以上红灯，5-30分黄灯。
2. 综合达成概率逻辑和样式修改，改成红、黄、绿灯的机制，如果不同渠道状态只要有1个绿灯，显示绿灯，如果没绿灯，有一个黄灯，就显示黄灯，其他就显示红灯。


# 任务：
1) 生成 docs/prd/changes/CR-YYYY-MM-DD-<模块名>.md 模板
2) 根据仓库现状，整理现有的逻辑，写需求变更文档
3) 根据需求变更文档，给出技术设计方案
4) 如何涉及到新API的，请给出新API的接口方案
5) 涉及到UI变化，给出组件修改的方案


# 输出要求：
- 不要创建模块以外的文件，需求和设计都在同一个文档修改
- 给出创建的文件路径
- 给出需要修改的代码文件，以及改动逻辑，包括新增和修改
- 不要开始实现功能和改代码